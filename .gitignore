# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Agent-generated documentation — local only, never committed
# These are outputs produced by scheduled and on-demand agents.
# The backlog (docs/backlog.md) is intentionally tracked — it's the source of truth for all agents.
docs/gap-reports/
docs/seo/
docs/content/
docs/analytics/
docs/daily-briefs/
docs/sprint-plans/
docs/sprints/dev-log.md
docs/sprints/qa-log.md
docs/specs/
docs/discovery/
docs/design/
docs/gtm/
docs/feedback-log.md

# Founder-authored strategy/planning scratch docs — local only, not site source
docs/ai-security-consolidation-plan.md

# Agent health heartbeat timestamps
.agent-health/*.last-run
!.agent-health/mcp-failures.log

# Local-only Claude Code harness state
.claude/launch.json
.claude/worktrees/

# Local-only / work-in-progress skill agents (not yet ready to commit)
.claude/skills/portfolio-gap-agent/
.claude/skills/portfolio-content-agent/
