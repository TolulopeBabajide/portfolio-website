# Code Review — 2026-05-18 · 916e175

**Verdict**: ✅ Clean
**Files reviewed**: 1 (src/components/Projects.jsx) + context (index.html, src/components/Hero.jsx)
**Commits covered**: a9c95bc, 04a9177, 916e175

## Summary Table
| Category | Findings | Worst Severity |
|----------|----------|----------------|
| JSX/React | 0 | 🟢 |
| Links & CTAs | 0 | 🟢 |
| SEO/Meta | 0 | 🟢 |
| Code Quality | 0 | 🟢 |
| Content Accuracy | 0 | 🟢 |
| Complexity | 2 | 🟡 |

## Findings

### src/components/Projects.jsx
- **🟡 Medium** Lines 90–235: `Projects` component function is ~145 lines, exceeds 60-line guideline. Contains three distinct rendering concerns: project card grid, project card internals, and CyberLabs section.
  Fix: Extract `ProjectCard` (renders a single card), `CyberLabsSection` (renders the Security Labs block), and keep `Projects` as a thin layout shell under ~30 lines (filed as M-19).

- **🟡 Medium** Lines 107–196: JSX nesting reaches 8–9 levels in the project card render area (section → div.grid → motion.div → div.aspect → conditional → div.absolute / div.p-5 → div.flex → div.flex → a), exceeding the 4-level guideline.
  Fix: Extract `ProjectCard` as described in M-19; this naturally reduces nesting to ≤4 levels within each sub-component (filed as M-20, resolved together with M-19).

## Backlog Items Filed
M-19 (🟡 define), M-20 (🟡 define)

## Notes
- The M-07 change (adding `"TypeScript"` to Awade's tags array at line 19) is correct and complete. Tag renders cleanly via the existing `project.tags.map(tag => <span key={tag}>)` at line 180. No JSX structure changes, no new imports, no side-effects.
- All asset imports verified: `awade-preview.png`, `planacle-preview.png`, `bookorbit-preview.png` all exist in `src/assets/`.
- `index.html` title and meta description remain correct.
- `Hero.jsx` Download CV link (`/resume.pdf target="_blank"`) is same-origin; `rel="noopener noreferrer"` absent but not required for same-origin links by checklist — noted in M-12 context, no new filing needed.
- M-19 and M-20 are the Projects.jsx counterparts to M-17/M-18 (Skills.jsx); dev-agent can tackle all four as a single extraction sprint.
- No 🔴 or 🟠 findings; branch is safe to proceed to QA.
