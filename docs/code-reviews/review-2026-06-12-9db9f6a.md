# Code Review — 2026-06-12 · 9db9f6a

**Verdict**: ⚠️ Refactor Recommended
**Files reviewed**: 2
**Commits covered**: e07a2ff, df77ba1, 9db9f6a

## Summary Table
| Category | Findings | Worst Severity |
|----------|----------|----------------|
| JSX/React | 0 | — |
| Links & CTAs | 1 | 🟠 |
| SEO/Meta | 0 | — |
| Code Quality | 0 | — |
| Content Accuracy | 0 | — |
| Complexity | 0 | — |

## Findings

### src/content/roles.js

- **🟠 High** Lines 56, 90, 127: Three role-specific `resumeUrl` values point to files that do not exist in `public/`:
  - `engineering` → `/resume-eng.pdf` (file never created)
  - `security` → `/resume-sec.pdf` (file deleted — staged as `D public/resume-sec.pdf` in git status)
  - `customer` → `/resume-cs.pdf` (file never created)

  `Hero.jsx:73` renders `href={config.resumeUrl}` directly with no fallback. A recruiter hitting `?role=engineering`, `?role=security`, or `?role=customer` and clicking "Download CV" gets a 404.

  Fix: Populate the missing PDFs in `public/`, or fall back each missing URL to `/resume.pdf` until the role-specific files are ready: e.g. `resumeUrl: '/resume.pdf'` for engineering and customer, and restore/replace `resume-sec.pdf` for security. Backlog item H-12 filed.

## Fix confirmed correct

The H-11 fix is correct. All four `seoTitle` values in `roles.js` now use `|` as separator (default, engineering, security, customer). The static `index.html` `<title>` was already using pipe; roles.js now matches. No `console.log`, no dead imports, no placeholder text in roles.js.

## Backlog Items Filed
- H-12: Role-specific resume PDFs missing — `/resume-eng.pdf`, `/resume-sec.pdf`, `/resume-cs.pdf` not present in `public/`

## Notes
The H-11 fix is a clean, complete change — the right scope and correct execution. The broken resume URLs pre-date this commit (introduced alongside role variants in `47c2264`) and surfaced during file-level review of `roles.js`. The `security` role case is the most urgent: the file existed (`d6f4a89`), was part of the public directory, and has since been deleted without restoring the `roles.js` reference.
