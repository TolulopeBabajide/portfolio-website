# Code Review — 2026-06-12 · f996977

**Verdict**: ✅ Clean
**Files reviewed**: 2 (`src/components/Experience.jsx`, `docs/backlog.md`)
**Commits covered**: d8975b3, 7e40a28, f996977

## Summary Table

| Category         | Findings | Worst Severity |
|------------------|----------|----------------|
| JSX/React        | 0        | 🟢             |
| Links & CTAs     | 0        | 🟢             |
| SEO/Meta         | 0        | 🟢             |
| Code Quality     | 0        | 🟢             |
| Content Accuracy | 0        | 🟢             |
| Complexity       | 2        | 🟡             |

## Findings

### src/components/Experience.jsx

- **🟡 Medium** Complexity — function length: `Experience` component (lines 81–212) is 132 lines, exceeding the 60-line guideline. The function handles three distinct concerns: the `orderedExperiences` sort, the scroll-spy `IntersectionObserver`, and the full JSX tree.
  Fix: Extract `ExperienceCard` (receives a single `exp` object, renders the timeline entry) and keep `Experience` as a thin layout shell (carouselRef, observer, dot indicators). This brings the component under 60 lines and matches the extraction pattern used in M-19/M-20 for Projects.

- **🟡 Medium** Complexity — JSX nesting: the description list render area reaches 7 levels of nesting (`section → div.max-w-7xl → div[carouselRef] → motion.div → motion.ul → motion.li → span`), exceeding the 4-level guideline.
  Fix: Resolved naturally by extracting `ExperienceCard` as described above — nesting within each sub-component stays ≤4 levels.

## Backlog Items Filed

M-35, M-36

## Notes

- M-34 implementation is clean: vertical timeline rule + period markers align correctly at `left-[15px]` / `left-0` with the `md:relative` parent providing the stacking context. Mobile carousel, dot indicators, `experienceOrder` sort, and WCAG-safe period label (`text-cyan-700 dark:text-cyan-400`) all verified present.
- Revenue figure consistent: single `₦1Bn` reference (line 63) — no ₦500M conflict.
- All imports resolve to existing modules (`RoleContext`, `SectionHeading`, `framer-motion`, `lucide-react`).
- Both complexity items (M-35, M-36) are flagged at 🟡 only; identical to the M-17/M-18 and M-19/M-20 pattern already in backlog. They should be tackled together in one extraction task.
